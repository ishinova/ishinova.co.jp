<section id="vision" class="vision">
  <div class="vision-background"></div>
  <div class="container">
    <div class="vision-content">
      <h2 class="section-title">
        <span class="title-decoration">ビジョン</span>
      </h2>
      <div class="vision-message">
        <blockquote>
          <p>
            「伝統と革新の間に境界線はない。今日の革新が明日の伝統となり、過去の叡智が未来の基盤となる。」
          </p>
        </blockquote>
        <div class="vision-text">
          <p>
            Ishinovaは、日本の伝統的な価値観と最先端テクノロジーの融合を目指す企業です。
            私たちは、和の美学とサイバーパンク的な未来感を組み合わせた独自の世界観で、
            次世代のスタンダードを創造します。
          </p>
          <p>
            過去と未来、伝統と革新、アナログとデジタル。
            相反するように見える概念の間に新たな価値を見出し、
            持続可能な社会の実現に貢献することがIshinovaの使命です。
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .vision {
    position: relative;
    color: white;
    padding: 3rem 0;
    overflow: hidden;
  }

  .vision-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: var(--color-indigo);
    background-image: 
      radial-gradient(circle at 20% 30%, rgba(212, 175, 55, 0.15) 0%, transparent 30%),
      radial-gradient(circle at 80% 70%, rgba(212, 175, 55, 0.1) 0%, transparent 30%);
    z-index: -1;
  }

  .vision-content {
    max-width: 100%;
    margin: 0 auto;
  }

  .section-title {
    text-align: center;
    margin-bottom: 2rem;
    font-size: 1.8rem;
    color: white;
  }

  .title-decoration {
    position: relative;
    display: inline-block;
  }

  .title-decoration::after {
    content: "";
    position: absolute;
    bottom: -8px;
    left: 0;
    width: 100%;
    height: 3px;
    background: linear-gradient(90deg, white, var(--color-gold));
  }

  .vision-message blockquote {
    font-size: 1.2rem;
    font-weight: 700;
    line-height: 1.5;
    margin: 0 0 1.5rem;
    padding: 0 1rem;
    border-left: 3px solid var(--color-gold);
    color: var(--color-gold);
  }

  .vision-text {
    font-size: 1rem;
    line-height: 1.7;
  }

  .vision-text p {
    margin-bottom: 1.2rem;
  }

  @media (min-width: 481px) {
    .vision {
      padding: 4rem 0;
    }

    .section-title {
      margin-bottom: 2.5rem;
      font-size: 2rem;
    }

    .vision-message blockquote {
      font-size: 1.4rem;
      margin: 0 0 2rem;
      padding: 0 1.5rem;
      border-left: 4px solid var(--color-gold);
    }

    .vision-text {
      font-size: 1.05rem;
    }
  }

  @media (min-width: 769px) {
    .vision {
      padding: 6rem 0;
    }

    .vision-background {
      background-image: 
        radial-gradient(circle at 20% 30%, rgba(212, 175, 55, 0.15) 0%, transparent 50%),
        radial-gradient(circle at 80% 70%, rgba(212, 175, 55, 0.1) 0%, transparent 50%);
    }

    .vision-content {
      max-width: 900px;
    }

    .section-title {
      margin-bottom: 3rem;
      font-size: 2.2rem;
    }

    .title-decoration::after {
      bottom: -10px;
    }

    .vision-message blockquote {
      font-size: 1.8rem;
      margin: 0 0 2.5rem;
      padding: 0 2rem;
    }

    .vision-text {
      font-size: 1.1rem;
      line-height: 1.8;
    }

    .vision-text p {
      margin-bottom: 1.5rem;
    }
  }
</style> 